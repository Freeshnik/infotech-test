[mysqld]
# 1. Explicitly load the legacy plugin library
plugin-load-add = mysql_native_password.so

# 2. Set the legacy plugin as the default for new users
default_authentication_plugin = mysql_native_password